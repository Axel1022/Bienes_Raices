extends ../layouts/index
block contenido
    div(class="bg-white shadow-lg py-8 rounded-lg max-w-md mx-auto")
        p(class=(error ? "bg-red-500" : "bg-green-500") + " text-white py-2 px-5 rounded-lg w-full text-center font-semibold")
            | #{mensaje}
        if !error
            a(href="/auth/login" class="text-center font-bold text-sm text-slate-600 uppercase mt-10 block") Ya puedes iniciar sesión
